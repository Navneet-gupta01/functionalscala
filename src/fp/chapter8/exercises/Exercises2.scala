package fp.chapter8.exercises

object MaxInListProperty {
  /*
   * 1. All elements in the list must be equal or less than the Max Element
   * 2. List with single ELement the max should eb that single element
   * 3. Max of Empty list hsould be an error or None if using option return type
   * 4. After Sorting in ascending order max should be last element in the list (Not necessary if equal element)*/
}